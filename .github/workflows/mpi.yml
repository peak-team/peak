name: MPI

on: [push, pull_request]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: false

      matrix:
        os: [ubuntu-22.04]
        build_type: [Release]
        c_compiler: [gcc]
        mpi: [ 'mpich', 'openmpi', 'intelmpi']

    steps:
    - name: Checkout
      uses: actions/checkout@v4      
    - name: Setup MPI
      uses: mpi4py/setup-mpi@v1
      with:
        mpi: ${{ matrix.mpi }}

    - name: Install test dependencies
      run: |
        sudo apt-get update
        sudo apt-get install --yes libopenblas-dev

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_CXX_COMPILER=mpicxx
        -DCMAKE_C_COMPILER=mpicc
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
        -DBUILD_TESTS=1
        -DPEAK_FETCH_DEPS=ON
        -DPEAK_ENABLE_FORTRAN_TESTS=ON
        -DPEAK_BUILD_BENCHMARKS=OFF
        -S ${{ github.workspace }}

    - name: Build
      # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}

    - name: Test
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      # Execute tests defined by the CMake configuration. Note that --build-config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      run: ctest --build-config ${{ matrix.build_type }} -LE "(benchmark|stress)" -V


  production-package:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Setup MPICH
      uses: mpi4py/setup-mpi@v1
      with:
        mpi: mpich
    - name: Configure production MPI package
      run: >
        cmake -S . -B mpi-package
        -DCMAKE_C_COMPILER=mpicc
        -DCMAKE_CXX_COMPILER=mpicxx
        -DCMAKE_BUILD_TYPE=Release
        -DCMAKE_INSTALL_PREFIX=${{ github.workspace }}/installed-mpi
        -DBUILD_TESTS=OFF
        -DBUILD_TESTING=OFF
        -DPEAK_ENABLE_MPI=ON
        -DPEAK_ENABLE_OTF2=OFF
        -DBUILD_CUDA_PROFILE=OFF
    - name: Build and install production MPI package
      run: |
        cmake --build mpi-package --parallel
        cmake --install mpi-package
    - name: Execute installed MPI package contract
      run: |
        cmake -S test/package_consumer -B package-consumer-mpi \
          -DCMAKE_PREFIX_PATH=${{ github.workspace }}/installed-mpi \
          -DCMAKE_DISABLE_FIND_PACKAGE_MPI=TRUE \
          -DPEAK_EXPECT_MPI_ENABLED=ON \
          -DPEAK_EXPECT_OTF2_ENABLED=OFF \
          -DPEAK_EXPECT_CUDA_ENABLED=OFF
        cmake --build package-consumer-mpi
        ctest --test-dir package-consumer-mpi --output-on-failure
